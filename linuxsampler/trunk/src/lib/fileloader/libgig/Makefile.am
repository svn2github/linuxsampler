# set the include path found by configure
INCLUDES = $(all_includes)

METASOURCES = AUTO

# to prevent compile errors on some systems
AM_CXXFLAGS = -pedantic -Wreturn-type -ffast-math -march=$(target_cpu)
